import { TodoFooter, TodoHeader, TodoList } from "../../molecules";

const TodoContainer = () => {

  return (<>
    <TodoHeader />
    <TodoList />
    <TodoFooter />
  </>);
};

export default TodoContainer;
